---
title: "Configurable clinical data harmonization with help from LLMs"
author: "Leslie Emery"
institute: "Bristol-Myers Squibb"
format: html
---

## 5 lines
1. Situation: Data harmonization is important but takes a lot of work and custom code.
1. Desire: I want to use LLM coding assistants to make this easier and faster.
1. Conflict: I can use e.g. Posit Assistant, but it will write code for me to do the data harmonization and when the data updates I'll have to do it all over again and it will be lots of code changes to review, with only a few key changes.
1. Change: data model (output format) + harmonization configs (inputs) + generalized code
1. Result: I have an LLM assisted data harmonization that's easy to update and do human review.

## Talk outline

1. Big picture description of a data harmonization project (e.g. 10 studies) and how much work goes into it
    - Diagram here of the many files from many studies, custom code for each input file to output transformation, lots of variation in the code, lots of repeating the same kinds of things over an over again
1. Big picture description of a data harmonization project using config files made by LLM with human review
    - Simpler diagram here of agent workflow
    - "I'm going to tell you about how I went from this [complicated diagram] to this [simple diagram]"
1. I built out this workflow based on my deep expertise in data harmonization
    - match input columns to desired output columns
    - figure out what format it's in
    - convert it to the format you want it to be
1. Data model (output)
    - Show prompt to get Posit Assistant to make the data model based on a group of studies
    - Show example data model
        - Structure:
            - _structure.yaml file; domain.yaml for each domain included
        - Domains: adverse_events, exposure, subjects, lab_tests, time_to_event, response
1. Harmonization configs (input)
    - Show example from subjects domain for 3 different studies
    - For each desired output column:
        - Which dataset & column to get it from
        - What data type is it
        - What units is it already in 
        - What categorical values exist
    - Mention relation to data dictionary? (of recent interest in field)
1. Config-based harmonization code
    - Show code snippets from subjects domain
    - Fully working for simple case of just renaming columns and converting data types
    - Still to handle (should I mention this or not?):
        - Converting units
        - Harmonizing categorical values
1. The harmonization config files maximize human attention/time spent reviewing LLM output
    - The configs capture the most important parts of the harmonization decisions -- which inputs map to which outputs, and what data transformations happen
    - Screenshot of the review app Burak has built
1. You get a great data dictionary for your output basically for free with the harmonization configs + data model
    - Clear definition of final output data
    - Links back to the input dataset + columns that were used
1. We've done this in a pilot project and it gets us pretty close to final state so we can focus our time on the hard parts
    - Internship, mention Eslam
1. We are building out a Claude Code plugin using our daapr framework
    - Mention Jason for plugin
    - Show plugin workflow design
1. You can use these approaches for other use cases
    - Any project where you're combining data from multiple sources (e.g. COVID data from different health authorities, published datasets from different reserach groups, eCommerce data from acquired companies, etc.)
    - Use data model .yaml to instruct LLM to make the output format you want
    - Use a config file that describes all of your inputs (makes it easier for LLM to reason about your data)
    - 

## Rhetoric for my talk

1. Logos: include use cases outside of pharma, e.g. ecommerce companies merging, academic research studies combining data, general purpose data cleaning, ???
2. Ethos: I've spent >10 years specializaing in data cleaning and harmonization, both at UW and at BMS
3. Pathos: many in audience will have experienced code breaking with data changes, or spending great amounts of time on data cleaning and reformatting
